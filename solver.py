class Solver:
    def __init__(self, word, length, guesses, slow, difficulty):
        self.word = word
        self.length = length
        self.guesses = guesses
        self.slow = slow
        self.difficulty = difficulty
    
    def solve(self):
        pass